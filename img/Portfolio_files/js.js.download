const array = [2, 7, 11, 15];

const twoSum = function (numbers, target) {
  for (var i = 0; i < numbers.length; i++) {
    for (var j = i + 1; j < numbers.length; j++) {
      if (target - numbers[j] === numbers[i]) return [i, j];
    }
  }
};

console.log(twoSum(array, 26));
